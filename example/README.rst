Usage
=====

.. code-block:: bash

   $ cd example
   $ python ps_pages_example.py

or

.. code-block:: bash

   $ cd example
   $ pip install -e .
   $ pserve development.ini --reload

and goto http://localhost:6543/admin/
